node_modules/
.next/
.env
.env.local
# Chặn mọi biến thể .env (backup, remote dump...) — trừ file mẫu.
# .env.production đã được track từ trước và chỉ chứa URL công khai.
.env.*
!.env.example
!.env.production
*.log
*.tsbuildinfo
.DS_Store
*.zip
*.pdf
repomix-output.*
src/image.png

# Test artifacts
playwright-report/
test-results/
/playwright/.cache/
coverage/

# ClaudeKit
.claude/
CLAUDE.md

# /flow run-state (generated; safe to ignore)
MODE
PROJECT_TYPE
.flow/
